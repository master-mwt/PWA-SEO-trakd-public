import { AddEscapesPipe } from './add-escapes.pipe';

describe('AddEscapesPipe', () => {
  it('create an instance', () => {
    const pipe = new AddEscapesPipe();
    expect(pipe).toBeTruthy();
  });
});
